import {isPositive} from "./04";

describe('isPositive()', () => {
    it('should return true when n > 0', () => {
        expect(isPositive(1)).toBe(true)
        expect(isPositive(10)).toBe(true)
        expect(isPositive(100)).toBe(true)
    })
    it('should return true when n > 0', () => {})
    it('should return true when n > 0', () => {})
})